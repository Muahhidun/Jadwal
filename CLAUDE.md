# Jadwal — исламское приложение напоминаний о поклонении

Этот файл автоматически читается Claude Code в начале каждой сессии. Он — точка входа для любого агента, продолжающего работу.

## Что это за проект
Мобильное приложение (Flutter, iOS + Android) — «ассистент поклонения»: напоминания, привязанные ко временам молитв (утренние/вечерние зикры, сура аль-Кахф и час дуа в пятницу), по тапу открываются сами зикры. Казахстан-first. Бесплатно, без рекламы, без бэкенда.

**Полный план, решения и архитектура: [PLAN.md](PLAN.md) — читать обязательно перед любой работой.**

## Жёсткие ограничения (нарушать нельзя)
1. **Никаких счётчиков-тасбихов** и цифровых счётчиков зикра — владелец считает это религиозным нововведением. Только отметка «выполнено» (галочка) на зикр целиком.
2. **Только достоверный религиозный контент** с указанием источника (сборник, номер хадиса) у каждого зикра. Религиозные тексты не придумывать и не «улучшать» — контент собирает и утверждает владелец.
3. **Никакой геймификации-самообмана**: без очков, бейджей, рейтингов. История/календарь постоянства — да.
4. Бесплатно, без рекламы, без сбора данных, всё хранится локально.

## Репозиторий
GitHub: https://github.com/Muahhidun/Jadwal (публичный, владелец Muahhidun). Удалёнка `origin` подключена, ветка `main`. Пуш: `git push origin main` (аутентификация — токеном владельца, вводит он сам). Первый деплой — 07.07.2026 (force-push поверх старого неактуального проекта).

## Роли
- Владелец (не программист): продукт, контент, финальные решения.
- Claude Code: весь код, дизайн, архитектура. Объяснять технические вещи простым языком.
- Общение — на русском.

## Очередь работ (друг отпал 15.07 — всё делает Claude Code владельца)
1. ✅ Иконка Дауам (терракота, буква даль) — 15.07.
2. ✅ Транскрипции зикров ДО КОНЦА + сворачиваемые блоки в читалке — 15.07.
3. ✅ Экран «Напоминания» (список вкл/выкл + конструктор своих) — 15.07.
4. ✅ Поиск городов по-русски (нормализация/синонимы в city.dart) — 15.07.
5. ✅ Privacy Policy (страница privacy.html в корне) — 15.07.
6. ✅ Хиджра: проверка API (ДУМК не отдает дату, оставлен расчет); смена даты хиджры ПОСЛЕ МАГРИБА (+1 день после захода) — 15.07.
7. Виджеты iOS (домашний+блокировка+StandBy+Today) → Apple Watch → Live Activity → Android-полировка → Mac.
Позже: проверка контента у специалиста (блокер релиза), аккаунты сторов, Android-устройство (владелец найдёт).

## Текущий статус (обновлять в конце каждой сессии!)
- **P1 по дизайн-отчёту владельца (сессия 19-20.07, СДЕЛАНО, на iPhone):** владелец сделал PDF «Dauam против Sajde» (~/Desktop/Dauam/). Реализовано: (1) **Тетрадь-кольца** — ячейка дня = кольцо прогресса (утро+вечер, пятница 4 задачи), пусто=серое неактивное, частично=золотая дуга, полное=кольцо с сиянием и заливкой; красные крестики убраны. (2) **Главный**: подпись НАД цифрами, адаптивный таймер (>1ч «1:54», <1ч «45 мин», <1мин «31 сек», AnimatedSwitcher-перелистывание); чипы «✓ Вечерние зикры» под таймером; контраст силуэта Мекки гарантирован по яркости неба (computeLuminance → тёмный/тёплый светлый). (3) **Настройки iOS**: `JColors.night` (тёплый тёмно-синий, НЕ чёрный — владелец против «белого на чёрном»), inset grouped группы, large title, без FAB (строка «+ Добавить»), bottom sheet вместо диалога. Решения владельца: намазы НЕ отмечаем (только зикры/свои напоминания); красного в тетради нет. Ключевые виджеты: `_RingCell`/`_RingPainter`, `_DoneChips`, `_InsetGroup`.
- **P2 (следующее):** Live Activity + Dynamic Island (отсчёт в островке), хаптика повсюду, живые переходы (numericText уже есть), параллакс за жестом. **P3:** виджеты/StandBy/Watch/будильник Фаджра (AlarmKit iOS 26)/Siri Shortcuts.

- **Этап:** 1 — разработка MVP (начат 04.07.2026).
- **Сделано (Этап 0):** план (PLAN.md), название Jadwal, все продуктовые решения. **Дизайн закрыт:** hi-fi прототип владельца в `design_handoff_jadwal/` (README.md — источник правды по UI). Шаблон контента `Jadwal — сбор зикров.xlsx` создан (5 вечерних зикров-примеров со статусом «черновик»).
- **Сделано (Этап 1, сессия 04.07):** git-репозиторий; Flutter-проект в `app/` (applicationId kz.jadwal; ios/android/web); Android SDK без Android Studio, `flutter doctor`: Flutter ✓, Android ✓; debug-APK собирается. Кириллический путь папки сборке не мешает.
- **Сделано (Этап 1, сессия 05.07):** владелец заполнил таблицу (39 зикров: 20 утренних + 19 вечерних, статус «черновик» — перед релизом проверка у специалиста). Конвейер контента: `tools/convert_adhkar.py` → `app/assets/data/adhkar.json`. Зависимости подключены (google_fonts, shared_preferences, intl, http, adhan, flutter_local_notifications, timezone, geolocator). Написан скелет приложения по хендоффу: `lib/theme/tokens.dart` (токены из README), `lib/i18n/strings.dart` (ru/kz из прототипа; в заголовках зикров — неразрывные пробелы!), `lib/data/` (adhkar-модели, AppState на shared_preferences), `lib/prayer/schedule.dart` (интерфейс + демо-провайдер: пятница 20:11 Алматы), экраны: онбординг 5 шагов, главный «одно дело» (3 состояния), «День и тетрадь» (демо-тетрадь), чтение-«книга», празднование. `flutter analyze` чисто, тесты проходят. Всё проверено визуально через `flutter run -d web-server` (порт 8735, `.claude/launch.json`) + Claude Preview: онбординг→главный→чтение→состояния — работают, скриншоты сняты. Найден и исправлен overflow сетки времён.
- **Сделано (Этап 1, сессия 05.07 — боевой слой времён):** времена молитв ДУМК (api.muftyat.kz/prayer-times/{год}/{lat}/{lng}, без ключа, год одним запросом) + adhan как офлайн-запас; `lib/prayer/`: city.dart (6 городов), muftyat.dart, schedule.dart (DayTimes + расчёт), schedule_service.dart (кеш→API→расчёт, ScheduleScope), windows.dart (движок окон + режим «после азана»). Главный/День/чтение переведены на живые времена, таймер тикает, дата по хиджре (пакет hijri). Тесты обновлены (widget + windows_test). Проверено в симуляторе iOS.
- **Окружение iPhone владельца:** iPhone **17 Pro** (iPhone18,1), iOS 26.5.1, по кабелю, Developer Mode ✓, Apple ID в Xcode ✓ (Team ZV5872VU8H, `muahhid@icloud.com`), подпись в проекте прописана. Симулятор iOS 26.5 работал (скриншот онбординга снят).
- **⚠️ ГРАБЛИ (не повторять):** диск Mac хронически забит (на старте ~18 ГБ, том `/System/Volumes/Data`). Нельзя удалять образы симулятора ради места командой `simctl runtime delete`: в Xcode 26 «платформа iOS» = SDK устройства + образ симулятора вместе, и удаление образа ломает **сборку на устройство** тоже (ошибка «iOS 26.5 is not installed» в `xcodebuild -showdestinations`). Лечение — `xcodebuild -downloadPlatform iOS` (снова качает ~8.5 ГБ). Для чистки места безопасны: `~/Library/Developer/Xcode/DerivedData/*`, `app/build`, `~/Library/Developer/Xcode/iOS DeviceSupport` (старые), кеши Homebrew.
- **Где остановились:** идёт `xcodebuild -downloadPlatform iOS` (восстановление платформы). После него — `flutter run -d 00008150-0008453A01D2401C --release` (это iPhone владельца). При успехе на телефоне появится приложение → владелец должен «Доверять разработчика»: Настройки → Основные → VPN и управление устройством → Apple ID → Доверять.
- **Сделано (правки владельца, сессия 05.07):** (1) геолокация geolocator + справочник 5695 пунктов ДУМК (tools/fetch_cities.py → assets/data/cities.json), авто-определение ближайшего города; (2) тап по городу → city_picker.dart (авто/поиск/список), онбординг-шаг тоже; (3) тап по дате → хиджра⇄григориан (сохраняется); (4) таймер посекундный Ч:ММ:СС; (6) счётчик повторов на кнопке чтения («1 раз/3 раза Дальше»). Модель города — координаты вместо индекса (kCities удалён, есть kDefaultCity/kMajorCities). Проверено в вебе на реальных данных Алматы.
- **Правка №5 (СДЕЛАНА):** главный↔день теперь вертикальная лента-скролл за пальцем (не шторка). `screens/home.dart` переписан как pager: AnimationController `_p` 0..1 через onVerticalDrag + fling-snap; общие элементы `_HeroTimer` (центр→верх) и `_HeroTimes` (морфинг 3+3→ряд из 6); слои `_HomeLayer` (уезжает вверх) и `_DayLayer` (въезжает снизу); `screens/scene_background.dart` — художественный фон на 2 экрана (ночь→рассвет, звёзды, полумесяц, параллакс), задел под сменные фоны в настройках темы. `day.dart` удалён (роль в pager). Геройские слои в IgnorePointer (иначе перехватывали тапы). Проверено в вебе: переход и морфинг работают в обе стороны. **Полировка на будущее:** контраст шапки на арт-фоне (сделана тень), возможно нужен верхний скрим; дата на «дне» не показана (только в шапке главного).
- **Полировка pager (сессия 06.07, по скринам владельца):** на светлой теме текст был невидим на ночном фоне → главный/день теперь **всегда светлый текст** (`const c = JColors.dark` в home), тема на этот экран не влияет (сменные фоны — будущая фича настроек). Вернул пропавшие шапку и подсказку свайпа (обёрнул слои в Positioned.fill; цвета подняты до c.sub). Убрал текст «свайп вниз назад» (налезал на таймер), развёл таймер по вертикали, добавил градиент-подложку под контент «дня» (читаемость) + дату/город на «дне». Фон приглушён (мягче рассвет, затемнение сверху под шапку). Кнопка чтения — счётчик в две строки по центру. Проверено в вебе.
- **Правки владельца раунд 2 (сессия 06.07, УСТАНОВЛЕНО на iPhone):** (2) высокие широты — запасной adhan переведён на `HighLatitudeRule.seventh_of_the_night` (было middle_of_the_night → Фаджр/Иша прижимались к полуночи, Экибастуз показывал 00:03/00:04). ВАЖНО: в тот момент API муфтията был недоступен (DNS ENOTFOUND и с сервера разработки, и, видимо, с телефона) → приложение показало фоллбэк; когда API отвечает — грузятся официальные времена. Фоллбэк отличается от муфтията до ~50 мин (для Алматы adhan Иша 20:50 vs муфтият 21:41) — это ожидаемо, он только запас. (1,3) «день» — отдельная тёплая панель #171812 со скруглённым верхом + золотая грань, полем до самого низа (ушла золотая полоса фона, экраны различимы). (4) таймер 78. (5) убрано «ОКНО ОТКРЫТО». (6) подпись (до молитвы) под таймером. (8) убрана кнопка «Тема». Проверено в вебе, собрано и установлено на iPhone (wireless flutter run подвисает на установке — ставил через `flutter install`, `timeout` на macOS нет).
- **Правки владельца раунд 3 (сессия 06.07):** главный экран = только крупный таймер (сетку времён и морфинг убрал — переход стал чистым); времена молитв перенесены на «день» сеткой 3×2 крупными плитками. Фон: `SceneBackground.customAsset` (String?) — задел под картинку владельца (он делает сам: портрет ~9:39, ~1290×5580, верх=главный/ночь, низ прикрыт панелью дня). Панель дня непрозрачная #171812 — когда будет картинка, обсудить полупрозрачность.
- **Движок уведомлений (сессия 06.07, СДЕЛАН, на iPhone):** `lib/notifications/notifications.dart` — flutter_local_notifications 22 + timezone + flutter_timezone. Планировщик `reschedule()` на 14 дней, cap 58 (лимит iOS 64): окна (в начале + за 30 мин до конца) + 5 намазов; выполненное окно сегодня не уведомляем. Тексты ru/kz внутри сервиса. Глобал `gNotifier` (nullable, null в тестах — иначе падают). `syncNotifications(app)` вызывается при смене города/языка/отметок (сигнатура-guard в home) и на первом кадре главного (+ запрос разрешения — для прошедших старый онбординг). Тап по уведомлению зикров → payload → reader. AppDelegate.swift: UNUserNotificationCenter delegate. Решения владельца: по умолчанию 4 окна + намазы вкл; в начале окна + напоминание перед концом; выполнено — молчать.
- **⚠️ КРИТИЧНЫЙ ФИКС ВРЕМЁН (сессия 06.07):** времена не совпадали с мечетью/Sajda (Экибастуз: Фаджр/Иша мимо на ~48 мин). Причина — `fetch_cities.py` округлял координаты до 5 знаков, а endpoint `prayer-times` ДУМК ищет город по ТОЧНОМУ совпадению координат (6 знаков) → 404 → фоллбэк adhan. Проверено: с точными координатами Экибастуз = Sajda минута в минуту (01:56/03:43/12:08/17:44/20:23/22:11). Фикс: координаты хранятся ТОЧНОЙ строкой (`City.latStr/lngStr`), передаются в URL дословно (`MuftyatApi.fetchYear(latStr,lngStr)`); cities.json перевыгружен без округления; kDefaultCity/kMajorCities с точными координатами ДУМК (Алматы было 43.238949 → 43.238293). AppState хранит город строками (новые ключи cityLatStr/cityLngStr). **Правило на будущее: никогда не округлять координаты ДУМК.**
- **Фон-картинка владельца (сессия 06.07):** владелец прислал `bg_dunes_1290x5580.png` (в корне проекта) — ночь/полумесяц/горы/рассвет, ровно 1290×5580 (1:4.33). Подключена: `app/assets/images/scene_bg.png` в pubspec, `SceneBackground.customAsset` = этот путь (Image.asset cover, errorBuilder → рисованный фон). Фон едет 1:1 с прокруткой (верх=главный, низ=день). «День» переделан на **бесшовный**: убраны золотая линия и скруглённая панель; вместо неё мягкий градиент-затемнение (#0B1512 0.0→0.8) без рамок — картинка проступает на обоих экранах. Кнопка «Тема» возвращена (владелец не просил убирать; темы переделаем позже — менять фон+цвет текста вместе), 3 кнопки влезают (контент уплотнён, сетка aspect 1.75).
- **Правки владельца раунд 4 (сессия 07.07):** (3,4) тетрадь постоянства — реальный календарь месяца (`_Notebook` в home, привязка к дням недели, `AppState.dayCompleted` = утренние+вечерние выполнены → зелёный, прошедший невыполненный → красный, сегодня → кольцо, будущее → пусто); убрана жёлтая «частично» (2 состояния). (6) главный: `_AlreadyBar` — полные названия + галочка вместо «СЕГОДНЯ УЖЕ». (1,5) баланс дня: hero-таймер день 0.13, контент 0.24. (7) **шрифты вшиты** (assets/fonts Manrope/Literata/Amiri, pubspec fonts, JType на fontFamily, google_fonts удалён) — нет мелькания при старте.
- **Осталось по отзыву:** (2) владелец считает «день» перегруженным — предложены варианты (3-й экран ниже / выездное меню / сворачиваемые блоки); пока сделал реальный календарь + баланс, ждём вердикт, нужен ли отдельный экран для тетради. Темы позже: менять фон+цвет текста вместе (тёмный текст на светлом фоне и наоборот).
- **Правки владельца раунд 5 (сессия 07.07):** (1) вшитый вариативный Manrope не применял вес → добавил `fontVariations` (wght) в JType — тонкий таймер как раньше. (2) вернул подскок подсказки свайпа (`_BouncingHint`, hintbounce — потерялся в рефакторе pager). (3) у выделенной молитвы убрана зелёная заливка `c.gdim` (остаток старой темы) → мягкая золотая рамка под тёмно-синий фон. (4) **таймер убран со второго экрана** (fade при p→1, не переезжает), сетка времён 3×2 поднята наверх «дня» (разгрузка плотности — важно, т.к. напоминаний станет больше 2). (5) кнопки Напоминания/Тема/Язык **закреплены внизу** (Positioned bottom вне прокрутки) — не уезжают за экран.
- **Динамическое небо, шаг 1 (сессия 08.07, на iPhone):** `scene_background.dart` полностью переписан в живую сцену по времени суток (привязка к временам молитв города): небо-градиент по фазам (ночь→рассвет→день→закат), солнце/луна плавно по дуге с зенитом на Зухре, звёзды + падающая звезда ночью, силуэт города-мечети на горизонте (купол/минареты/деревья), ниже — город, который ночью оживает (горят окна, светятся фонари). `SkyFg`/`skyForeground(t,nowSec)` — авто-цвет текста главного экрана (днём тёмный, ночью светлый). home.dart передаёт `times`/`nowSec` в фон, главный текст через fg. Картинка-дюны заменена сценой. Согласовано с владельцем через Claude Design: горизонт = город+мечеть (стиль Медины), нижний экран = продолжение города с ночными огнями.
- **Динамическое небо, шаг 2 (СЛЕДУЮЩЕЕ):** переверстать экран «день» под макет Claude Design, который владельцу нравится: 6 времён **вертикальным списком** (не 3×2), все блоки — **стеклянные карточки** (полупрозрачный фон + рамка), задачи с галочкой/пустым кругом, календарь-тетрадь, заголовок «ТЕТРАДЬ ПОСТОЯНСТВА». Скрин макета у владельца.
- **Разделение работы:** владелец (Claude Code) — визуал/небо/темы; друг — Напоминания + поиск городов по-русски (см. раздел «Параллельная работа»). Репозиторий: github.com/Muahhidun/Jadwal.
- **Работа из Antigravity (сессия ~09-10.07, закоммичена 10.07):** владелец дорабатывал проект в Antigravity: приложение **переименовано Jadwal → Дауам** (دوام; Info.plist, AndroidManifest, онбординг, тексты ru/kz), новые иконки приложения, `mecca_silhouette.png`, переработаны home.dart/scene_background.dart (шаг 2: стеклянные карточки дня, «ТЕТРАДЬ ПОСТОЯНСТВА»). Работа была НЕ закоммичена — Claude Code закоммитил (c40c67c), починил тест (ожидал «Jadwal») и запушил вместе с динамическим небом. GitHub актуален.
- **Где остановились:** Сделаны русскоязычный поиск городов, политика конфиденциальности, автоматический переход даты хиджры после Магриба. Название возвращено к **Dauam**. Иконка сгенерирована из кропа владельца с безопасными отступами (820px по высоте, центрирована). Убран дублирующий `_AlreadyBar` снизу; статус выполнения активного зикра перенесен в центр экрана (`✓ Выполнено` / `✓ Орындалды`) вместо кнопки. Подсказка свайпа завернута в стеклянную плашку для читаемости. Исправлен баг с линией среза солнца на горизонте (солнце теперь плавно заходит за округлые силуэты дюн).
- **Следующее:** Разработка **виджетов iOS** (рабочий стол, экран блокировки, StandBy, Today View) и расширения для **Apple Watch**.
- **⚠️ Знать про сборку на iPhone:** По кабелю или Wi-Fi, при успешном `Xcode build done` можно ставить через `flutter install -d <id>`.
- **Ждём от владельца:** Освободить 30–40 ГБ на диске под разработку WidgetKit и watchOS-приложения (сборка дополнительных таргетов требует места).
